# Testing

This is where we put all the testing files to ensure that the stuff we developed works as intended. Can be examples or something simple that runs the code. 

For more information, google unit testing and `pytest`.